package net.neoforged.neoforge.api.distmarker;

/**
 * Compatibility shim for Dist marker. This is a minimal placeholder used only to satisfy
 * compile-time imports in this project. It intentionally contains no behaviour.
 */
public enum Dist {
    CLIENT,
    DEDICATED_SERVER;
}
